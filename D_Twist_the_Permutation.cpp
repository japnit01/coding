#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define all(x) x.begin(), x.end()
#define vi vector<int>

void rotateleft()

void solve() 
{
    int n;
    cin>>n;

    vector<int> A(n);
    for(int i=0;i<n;i++){
        cin>>A[i];
    }

    vector<int> B(n);
    for(int i=n;i>0;i--)
    {
        for(int j=0;j<n;j++)
        {
            if(A[j] == i)
            {
                n-
            }
        }
    }
}

int main()
{
    int t;
    cin>>t;

    while(t-- > 0)
    {
        solve();
        
        cout<<"\n";
    }
}
